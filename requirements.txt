# Core requirements (always needed)
faiss-cpu>=1.7.0
numpy>=1.21.0
pandas>=1.3.0
requests>=2.25.0
tqdm>=4.60.0

# For full functionality (optional but recommended)
huggingface-hub>=0.16.0
Pillow>=9.0.0

# For WD14 tagger support (optional)
# Note: This will install GPU version if CUDA is available
onnxruntime-gpu>=1.15.0 ; platform_system != "Darwin"
onnxruntime>=1.15.0 ; platform_system == "Darwin"  # Mac doesn't support onnxruntime-gpu

# For CLIP/SigLIP model support (optional)
# Note: These can be large, install only if needed
# flax>=0.7.0
# jax>=0.4.0
# jaxlib>=0.4.0

# Alternative: For PyTorch-based inference (lighter than JAX)
# torch>=2.0.0
# safetensors>=0.3.0